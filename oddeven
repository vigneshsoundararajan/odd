#include <stdio.h>
 
void main()
{
	int number;
	printf("enter a number to be check");
	scanf("%d",&number);
	if(number%2==0)
	{
		printf("\n the given number is even");
 
	}
	else
	{
		printf("\n the given number is odd");
 
	}


}
