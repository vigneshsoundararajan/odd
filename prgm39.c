#include<stdio.h>
void main()
{
	int g,i,n;
	int a[10];
	printf("\n Enter the numbers");
	for(i=1;i<10;i++)
	scanf("%d",&a[i]);
	a[0]=g;
	for(i=1;i<10;i++)
	{
		if(a[i]>g)
		{
		n=a[i];
		}
		
	}
		printf("\n the lagest among the number is %d",n);
}
